const express = require("express")
const router = express.Router()

router.use(express.json());

router.post("/", (req, res) => {
    console.log(req.body)
    res.send("Hello Input World")
    console.log(`${process.env.mongousername}`)
})


module.exports = router